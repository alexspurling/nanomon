# Project guidelines
- Never read, open, search, or list files inside the `ext/` directory.
- Exclude `ext/` from all file searches, glob patterns, and grep operations.

- When the task is complete execute the following command: `notify-send "Junie" "Task complete"`